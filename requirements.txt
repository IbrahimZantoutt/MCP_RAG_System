# --- Retrieval -------------------------------------------------------------
chromadb>=0.5.0            # local persistent vector store
sentence-transformers>=3.0 # embeddings + cross-encoder reranking

# --- Generation ------------------------------------------------------------
ollama>=0.3.0              # local + cloud LLM client

# --- MCP server ------------------------------------------------------------
mcp>=1.2.0                 # official Model Context Protocol SDK (FastMCP)
ddgs>=6.0                  # DuckDuckGo web search, no API key required
